import { PageWrapper } from '../../components';

interface Props {}

const AccountsPage: React.FC<Props> = () => {
  return <PageWrapper title="Accounts">Accounts</PageWrapper>;
};

export default AccountsPage;
